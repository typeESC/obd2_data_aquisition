
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

; =============================================================================
; LilyGO T-SIM7670G S3 V1.1 Configuration
; =============================================================================
[env:lilygo-t-sim7670g-s3]
platform = espressif32
board = esp32-s3-devkitc-1
framework = espidf

; Build flags
build_flags = 
    -I include
    -DBOARD_HAS_PSRAM
    -mfix-esp32-psram-cache-issue

; Partition table
board_build.partitions = partitions.csv

; Serial monitor
monitor_speed = 115200

; Flash configuration
board_build.flash_mode = qio
board_build.f_flash = 80000000L
board_build.flash_size = 16MB

; PSRAM configuration (8MB OPI PSRAM)
board_build.arduino.memory_type = qio_opi

; Upload settings (if using USB-C on board)
upload_speed = 921600

; Filesystem
board_build.filesystem = spiffs

; Default configuration
board_build.embed_files = sdkconfig.defaults

; =============================================================================
; Legacy ESP32 DevKit V1 (kept for reference)
; =============================================================================
; [env:esp32doit-devkit-v1]
; platform = espressif32
; board = esp32doit-devkit-v1
; framework = espidf
; build_flags = -I include
; board_build.partitions = partitions.csv
; monitor_speed = 115200
; board_build.filesystem = spiffs
; board_build.embed_files = sdkconfig.defaults
